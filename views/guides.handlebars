

<div align="center">
    <br>
    <br>
    <h2> "Interested in moving to a new location with a different climate? Our guides can help you!" </h2>
    <h2>To see our list of Guides, select your desired climate from the drop-box below, then press search.</h2>
</div>

<div align="center">

        <label for='climate_filter'></label><select id='climate_filter' name="climate">
            {{#each climates}}
                <option value="{{climate}}">{{climate}}</option>
            {{/each}}
        </select>
        <br>
        <br>
        <input type="button" value="search climate" onclick="filterGuidesByClimate()">
        <br>
        <br>

    <table>
        <thead>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Zip Code</th>
        </thead>
        <tbody>
                {{#each guides}}
                <tr>
                    <td>{{firstName}}</td>
                    <td>{{lastName}}</td>
                    <td>{{email}}</td>
                    <td>{{zipCode}}</td>
                </tr>
                {{/each}}
        </tbody>
    </table>
</div>

<script src="filterguides.js"></script>
